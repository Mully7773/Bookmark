import { StyledMainWrapper } from '../../styles/Main/StyledMainWrapper';

const MainWrapper = ({ children }) => {
  return <StyledMainWrapper>{children}</StyledMainWrapper>;
};

export default MainWrapper;
